#include<stdio.h>
#include<stdlib.h>

struct Node {
	int data;
	struct Node * left ;
	struct Node * right;
}, * root;
Node * InsertData(Node *);
int height (Node * );

Node 
int main (){
	return 0;
}
